# Frontend API base URL
# Use localhost when running frontend and backend on the same machine/browser.
# If testing from another device, use your machine LAN IP, for example:
# Production example:
VITE_API_URL=https://terralief-be.byhan.id
VITE_API_URL=http://localhost:3001