{
  "name": "backend",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun test tests/core && bun test tests/services && bun test tests/controllers",
    "db:generate": "bun x --bun drizzle-kit generate",
    "db:migrate": "bun x --bun drizzle-kit migrate",
    "db:studio": "bun x --bun drizzle-kit studio"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.5.0",
    "@scalar/hono-api-reference": "^0.10.0",
    "drizzle-orm": "^0.41.0",
    "hono": "^4.12.5",
    "jose": "^6.0.10",
    "pino": "^9.6.0",
    "pino-pretty": "^13.1.3",
    "postgres": "^3.4.5",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^22.14.1",
    "drizzle-kit": "^0.30.6"
  }
}